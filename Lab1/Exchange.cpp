#include "Exchange.h"

double Exchange::exchange(const Currency& c1, const Currency& c2, double amount)
{
	/*if (c2.getCode() == "PLN") {
		if (c1.getCode() == "PLN") {
			return amount;
		}
		else {
			return 0.0;
		}
	}
	else {
		Exchange::exchange()
	}
	exchange()
	return 0.0;*/
	return amount;
}
